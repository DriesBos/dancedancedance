# Agents Notes

- Keep performance tracking in `docs/optimisation-history.md` and update it whenever an optimization is shipped.
- Do not implement reduced motion preference handling (`prefers-reduced-motion`) unless explicitly requested by the user.
